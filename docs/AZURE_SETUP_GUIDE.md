# Azure Setup Guide (stub)

> 🚧 This guide will be fully authored together with **Module 3** (FastAPI, CI/CD & First Azure
> Deploy) — see [PLAN.md](../PLAN.md).

It will cover:

- Creating a free Azure account safely (spending limits, cost alerts — so you can't get surprise bills)
- Installing and logging in with the Azure CLI (`az`)
- Resource groups, Azure Container Registry, Azure Container Apps (free/consumption tier)
- Wiring GitHub Actions to Azure with OIDC (no long-lived secrets)
- Tearing everything down (`az group delete`) so nothing keeps costing money
